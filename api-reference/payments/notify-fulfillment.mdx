---
title: "Notify Fulfillment"
openapi: "POST /v1/payments/{payment_id}/notify-fulfillment"
---

Notify Yuno that a payment's order has been fulfilled. This updates the payment record with fulfillment status information for tracking and compliance purposes.

## When to use

Use this endpoint after you have fulfilled the order associated with a payment -- for example, after shipping a physical product or delivering a digital good. This helps maintain accurate order lifecycle tracking.

## Authentication

| Header | Type | Required | Description |
|---|---|---|---|
| `public-api-key` | string | Yes | Your public API key from the Yuno dashboard |
| `private-secret-key` | string | Yes | Your private secret key from the Yuno dashboard |

## Request example

<CodeGroup>

```bash cURL
curl -X POST https://api-sandbox.y.uno/v1/payments/86af11d8-4709-41c2-a048-aaa982ef7765/notify-fulfillment \
  -H "public-api-key: YOUR_PUBLIC_API_KEY" \
  -H "private-secret-key: YOUR_PRIVATE_SECRET_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```

```javascript Node.js
const paymentId = "86af11d8-4709-41c2-a048-aaa982ef7765";

const response = await fetch(
  `https://api-sandbox.y.uno/v1/payments/${paymentId}/notify-fulfillment`,
  {
    method: "POST",
    headers: {
      "public-api-key": "YOUR_PUBLIC_API_KEY",
      "private-secret-key": "YOUR_PRIVATE_SECRET_KEY",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({}),
  }
);
```

```python Python
import requests

payment_id = "86af11d8-4709-41c2-a048-aaa982ef7765"

response = requests.post(
    f"https://api-sandbox.y.uno/v1/payments/{payment_id}/notify-fulfillment",
    headers={
        "public-api-key": "YOUR_PUBLIC_API_KEY",
        "private-secret-key": "YOUR_PRIVATE_SECRET_KEY",
        "Content-Type": "application/json",
    },
    json={},
)
```

</CodeGroup>

## Key parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `payment_id` | string (UUID v4) | Yes | Path parameter. The payment whose order has been fulfilled |

## Error responses

| Status | Code | Description | Resolution |
|---|---|---|---|
| 400 | `INVALID_PARAMETERS` | Invalid UUID format | Ensure `payment_id` is a valid UUID v4 |
| 401 | `INVALID_CREDENTIALS` | Missing or invalid API keys | Verify your API key headers |
| 403 | `AUTHORIZATION_REQUIRED` | Merchant not authorized | Contact Yuno support |
| 404 | `NOT_FOUND` | Payment does not exist | Verify the `payment_id` is correct |

## Related endpoints

- [Get Payment](/api-reference/payments/get) - Check payment status
- [Create Payment](/api-reference/payments/create) - Create a new payment
- [Capture Payment](/api-reference/payments/capture) - Capture an authorized payment before fulfillment
